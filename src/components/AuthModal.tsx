'use client'

type PropType = {
    open: boolean;
    onClose: () => void;
};

const AuthModal = ({ open, onClose }: PropType) => {
    return (
        <>
            AuthModal
        </>
    );
};

export default AuthModal;
