type propType={
    open:boolean,
    onClose:()=>void
}
const AuthModal = ( {open,onClose}:propType) => {
    return (
        <>
            AuthModal
        </>
    );
};

export default AuthModal;
